name: Phase‑3.5 Invariants

on: [push, pull_request]

jobs:
  check-invariants:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      # Fix 1: Scan the correct runtime directory (nova_backend/src/)
      - name: No legacy EXECUTION_ENABLED in runtime
        shell: pwsh
        run: |
          $hits = Get-ChildItem -Path .\nova_backend\src\ -Recurse -File |
                  Select-String -CaseSensitive -Pattern "\bEXECUTION_ENABLED\b" |
                  Where-Object { $_.Path -notmatch "archive_quarantine|tests|__pycache__" }
          if ($hits) { Write-Error "Found EXECUTION_ENABLED in runtime: $hits"; exit 1 }

      - name: No execute_action( calls
        shell: pwsh
        run: |
          $hits = Get-ChildItem -Path .\nova_backend\src\ -Recurse -File |
                  Select-String -Pattern "execute_action\(" |
                  Where-Object { $_.Path -notmatch "archive_quarantine|tests|__pycache__" }
          if ($hits) { Write-Error "Found execute_action( call: $hits"; exit 1 }

      - name: No quarantine imports
        shell: pwsh
        run: |
          $hits = Get-ChildItem -Path .\nova_backend\src\ -Recurse -File |
                  Select-String -Pattern "archive_quarantine|phase35_execution" |
                  Where-Object { $_.Path -notmatch "tests|__pycache__" }
          if ($hits) { Write-Error "Found quarantine import: $hits"; exit 1 }

      # Fix 2: Make the Python test aware of the nova_backend package
      - name: ExecuteBoundary fails closed
        shell: pwsh
        run: |
          python -c "
import sys
import os
# Add nova_backend to the Python path so 'src' is found.
sys.path.insert(0, os.path.join(os.getcwd(), 'nova_backend'))
import importlib
eb = importlib.import_module('src.governor.execute_boundary.execute_boundary')
b = eb.ExecuteBoundary()
assert b.allow_execution() is True
previous = eb.GOVERNED_ACTIONS_ENABLED
eb.GOVERNED_ACTIONS_ENABLED = False
try:
    assert b.allow_execution() is False
finally:
    eb.GOVERNED_ACTIONS_ENABLED = previous
print('PASS: ExecuteBoundary is fail-closed when execution is disabled')
"
