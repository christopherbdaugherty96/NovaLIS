# Full Repo / Doc / Code Alignment Audit

**Date:** 2026-05-11
**Branch:** audit/full-repo-doc-code-alignment
**Main HEAD audited:** 40e704a (docs: lock audit-first safety boundary)
**Audit type:** Read-only. No runtime changes. No capability expansion.

---

## Audit Scope

1. Runtime/generated truth reconciliation
2. Capability registry ↔ docs alignment
3. Governance authority/bypass audit
4. UI/WebSocket/routing audit
5. Future docs/planning-only labeling audit
6. Proof/test/CI claims audit
7. Stale/duplicate docs cleanup map
8. Cross-cutting observations

---

## Area 1 — Runtime/Generated Truth Reconciliation

**PASS** — Generation script is correctly identified. `CURRENT_RUNTIME_STATE.md` line 4:
`Generated By: scripts/generate_runtime_docs.py`. `.github/workflows/runtime-docs.yml`
enforces via CI (`git diff --exit-code` on committed outputs).

**GAP (P1)** — 10 files are stale relative to HEAD. `git status` shows uncommitted changes in:

- `docs/current_runtime/CURRENT_RUNTIME_STATE.md`
- `docs/current_runtime/RUNTIME_FINGERPRINT.md`
- `_MOCs/BY_PHASE.md`, `BY_TOPIC.md`, `BY_TYPE.md`, `CODE_BY_LAYER.md`,
  `CODE_MODULES.md`, `HOME.md`, `RECENT.md`, `REPO_BY_FOLDER.md`

Cause: `scripts/generate_runtime_docs.py` was not re-run after PR #140 (slice-9) merged
`tests/websocket/test_session_layer_pipeline.py` and removed dead code in
`governor_mediator.py`. The drift is generator lag — not a manual edit violation.
`CURRENT_RUNTIME_STATE.md` line 7 explicitly prohibits manual edits.

**Risk:** `runtime-docs` CI workflow will fail on the next push to any branch until the
generator is re-run and committed.

---

## Area 2 — Capability Registry ↔ Docs Alignment

**PASS** — 27 capabilities in `registry.json`. `CURRENT_RUNTIME_STATE.md` lists exactly 27
(IDs 16–22, 31–32, 48–65). Count is consistent.

**PASS** — Cap 16 (`governed_web_search`): `status=active`, `enabled=True`,
`authority_class=read_only_network`, `requires_confirmation=False`. Correctly described
in docs. PR #134 cited as certification lock in AGENTS.md, CURRENT_WORK_STATUS.md,
ACTIVE_TODO.md.

**GAP (P2)** — Cap 16 `authority_scope` field absent from `registry.json`. The `Capability`
dataclass in `capability_registry.py` line 54 defaults this to `"suggest"`. The field is
not explicitly set in the registry JSON, creating an implicit dependency on the dataclass
default. Not a functional bug — routing is unaffected — but a registry completeness gap.

**PASS** — Cap 64 (`send_email_draft`): `requires_confirmation=True`,
`authority_class=persistent_change`, `enabled=True`. Runtime state doc correctly describes
this as local `mailto:` draft only, user-initiated send, no autonomous SMTP. Note: the
`authority_class=persistent_change` does not encode the "local-only" constraint explicitly —
the constraint lives in the description and route handler, not the registry class.
Not a misalignment, but a transparency note.

**PASS** — Cap 65 (`shopify_intelligence_report`): `authority_class=read_only_network`,
`requires_confirmation=False`, `enabled=True`. Correctly described as read-only.
No Shopify write authority.

---

## Area 3 — Governance Authority/Bypass Audit

**PASS** — `governor_mediator.py` lines 1–80: imports are clean (stdlib only). No external
imports or direct registry access at module level. `CapabilityRegistry` import is lazy
(inside `_load_enabled_capability_ids()` at line 75).

**PASS** — `session_handler.py` lines 1–60: no direct capability calls or registry imports.
`governance_refusal_for()` at line 47 actively blocks `cap63`, `bypass governor`, and
`GovernorMediator` injection strings before routing.

**PASS** — Grep for `bypass`, `direct`, `shortcut` in `src/`: all hits are enforcement
tooling, archived/disabled code (`archive_quarantine/`), or local file-path resolution
(`brain_server.py` line 2055: `direct_path = _resolve_existing_local_path(...)`).
No active governance bypasses found.

**FLAG (P2)** — `nova_backend/src/api/openclaw_agent_api.py` lines 24 and 45 reference
`OPENCLAW_DEPRECATED_DIRECT_RUN` as a deprecation log key. The path is logged but not
confirmed hard-blocked in this audit. The flag-off branch body was not fully read.
Warrants a targeted inspection: if the deprecated path can still execute when the flag is
set, that is a governance boundary violation.

---

## Area 4 — UI/WebSocket/Routing Audit

**PASS** — `HELP_ORIENT_RE` in `intent_patterns.py` lines 60–71 correctly includes:

```python
r"help\s+me"
r"i\s+(?:need|want)\s+(?:some\s+)?help"
r"can\s+you\s+help(?:\s+me)?"
r"could\s+you\s+help(?:\s+me)?"
r"i\s+(?:could\s+)?use\s+(?:some\s+)?help"
```

The RC-7 slice-9 fix is present. "i want help" is covered by `i\s+(?:need|want)`.

**PASS** — `PHRASE_NORMALIZATION` in `response_style_router.py` lines 87–107 does not
contain conflicting "help me" entries. Lines 98–101 include an explicit comment:
`"Note: 'help me', 'i need help', 'can you help me', 'where do i start' are intentionally
NOT normalized here. They route to HELP_ORIENT_RE (RC-7 warm orienting question)."`
RC-7 fix is correctly coded and documented.

**PASS** — `"get started"` and `"how do i start"` normalize to `"what can you do"`.
`"where do i start"` correctly routes to `HELP_ORIENT_RE`. Boundary is clean.

---

## Area 5 — Future Docs/Planning-Only Labeling Audit

**PASS** (verified) — The following future docs carry correct planning-only status lines:

- `docs/future/NOVA_TWO_DOMAIN_DIRECTION_2026-05-11.md` line 4:
  `Status: future direction / planning record / non-runtime-authority`
- `docs/future/GOOGLE_READ_ONLY_CONNECTOR_FOUNDATION_2026-05-03.md` line 4:
  `Status: Planning only — no Google OAuth, no Gmail API, no Calendar API runtime
  connector exists.`
- `docs/future/NOVA_GOOGLE_CONNECTOR_MODEL.md`: correctly framed as directional.

**UNVERIFIED (P4)** — Approximately 6 Google-connector future docs were not individually
read for planning-only headers:

- `docs/future/GOOGLE_CONNECTOR_IMPLEMENTATION_ROADMAP.md`
- `docs/future/NOVA_GOOGLE_ACCOUNT_AND_CONNECTOR_ONBOARDING_PLAN.md`
- `docs/future/NOVA_GOOGLE_CONNECTOR_DIRECTION_FINAL_LOCK_2026-04-26.md`
- `docs/future/NOVA_MCP_GOVERNED_CONNECTOR_PLAN_2026-04-27.md`
- (other connector siblings)

**FLAG (P4)** — `docs/future/NOVA_GOOGLE_CONNECTOR_DIRECTION_FINAL_LOCK_2026-04-26.md`
has "Lock" in its filename. Similar to `docs/status/ACTIVE_PRIORITY_LOCK_*.md` naming.
A reader could misread this as a governance priority lock rather than a future planning doc.
The file header was not confirmed to carry a planning-only disclaimer in this audit.
Recommend adding `Status: planning only — not a runtime authority lock` to line 1–5 if
not already present.

---

## Area 6 — Proof/Test/CI Claims Audit

**PASS** — `tests/websocket/test_session_layer_pipeline.py` exists and passes:
`98 passed in 0.30s` (run against current working tree, 2026-05-11).

**PASS** — `EVERYDAY_UX_FRICTION_CLOSEOUT_2026-05-11.md` lines 79–85 claim:
- 536 websocket+conversation tests pass
- 98 new full-pipeline tests pass
- 26/26 verification scenarios pass

The 98-test count matches live pytest run. The 26/26 claim is consistent with PR #139.

**UNVERIFIED (P5)** — The full 536-test websocket+conversation suite was not run in this
audit. The count is taken from the closeout doc. A full suite run before the next
implementation PR would confirm no regression.

**PASS** — Six CI workflow files confirmed:
`.github/workflows/ci.yml`, `runtime-docs.yml`, `governance-check.yml`,
`fingerprint-clean-main-pr.yml`, `phase-3.5-verification.yml`, `phase35_invariants.yml`.

**GAP (P1, same as Area 1)** — `runtime-docs.yml` will fail on next push until generator
is re-run. Not new — pre-existing from before this audit branch.

---

## Area 7 — Stale/Duplicate Docs Cleanup Map

**`docs/status/` — 13 files:**

| File | Status |
|---|---|
| `ACTIVE_PRIORITY_LOCK_2026-05-04.md` | Marked completed — OK |
| `ACTIVE_PRIORITY_LOCK_2026-05-06_RUNTIME_TRUTH_AUDIT.md` | Marked completed — OK |
| `ACTIVE_PRIORITY_LOCK_2026-05-06_TRUST_REVIEW_CARD_MVP.md` | Not checked in detail |
| `ACTIVE_PRIORITY_LOCK_2026-05-06_WEB_NEWS_PROOF_STRESS_TEST.md` | Qualified closed — OK |
| `CURRENT_WORK_STATUS.md` | Current (2026-05-11) — OK |
| `LOCAL_CAPABILITY_SIGNOFF_MATRIX_2026-05-06.md` | Historical snapshot — OK |
| `OPENCLAW_PRIORITY_LOCK_CLOSEOUT_2026-05-06.md` | Closeout — OK |
| `README.md` | Not checked in detail |
| `REPO_BRANCH_AND_WORKSTREAM_STATUS_2026-05-01.md` | Historical snapshot, no "superseded" label |
| `TRUST_REVIEW_CARD_MVP_CLOSEOUT_REVIEW_2026-05-07.md` | Closeout — OK |
| `TRUST_REVIEW_CARD_MVP_STATUS_2026-05-07.md` | **FLAG (P3)** — still says "implementation proof / review required" |
| `WEB_NEWS_UI_PROOF_LOCK_CLOSEOUT_REVIEW_2026-05-07.md` | Closeout — OK |
| `WORKFLOW_STAGE_ROADMAP_2026-05-02.md` | **FLAG (P3)** — says OpenClaw lock is active (it closed PR #107) |

**FLAG (P3)** — `TRUST_REVIEW_CARD_MVP_STATUS_2026-05-07.md`: says
`Status: implementation proof / review required`. The closeout review doc
(`TRUST_REVIEW_CARD_MVP_CLOSEOUT_REVIEW_2026-05-07.md`) exists and classifies the MVP as
`merged / display-only / non-authorizing / follow-ups tracked`. The status doc was not
updated to reflect the closeout. Creates a split-truth risk.

**FLAG (P3)** — `WORKFLOW_STAGE_ROADMAP_2026-05-02.md`: says
`"Priority lock active 2026-05-04: OpenClaw hardening sequence only"`. That lock is
completed (PR #107). The doc is dated (a snapshot), but carries no explicit
`Superseded by:` label. Recommend adding one.

**`docs/todo/` — 5 files:**

| File | Status |
|---|---|
| `ACTIVE_TODO.md` | Current (2026-05-11) — OK |
| `DOC_CLEANUP.md` | Not checked in detail |
| `README.md` | Not checked in detail |
| `SHOPIFY_SETUP_TODO.md` | Planning-only — OK |
| `TECH_DEBT.md` | Not checked in detail |

**`docs/PROOFS/` top-level (3 files):**
- `CAPABILITY_VERIFICATION_AUDIT_2026-03-25.md`
- `Governor Bypass Proof Document.txt`
- `README.md`

Subdirectories (not listed in top-level glob but confirmed to exist):
`docs/PROOFS/Everyday-UX/`, `docs/PROOFS/Web-News-Reporting/`, `docs/PROOFS/UI-Commands/`

---

## Area 8 — Cross-Cutting Observations

**PASS** — `AGENTS.md` is current. Names audit as active task. Prohibits Cap 64 P5,
Google connector runtime, Shopify writes, ElevenLabs, UI simplification, OpenClaw
expansion without separate reviewed locks.

**PASS** — `README.md` is current. References two-domain direction, links to planning docs
correctly, links to CURRENT_RUNTIME_STATE.md and CURRENT_WORK_STATUS.md.

**PASS** — No document found claiming Cap 64 P5 is active or approved. AGENTS.md line 36
explicitly prohibits starting it without a separate lock.

**PASS** — No document found claiming Google connector is implemented at runtime.
`GOOGLE_READ_ONLY_CONNECTOR_FOUNDATION_2026-05-03.md` is explicit: "no Google OAuth, no
Gmail API, no Calendar API runtime connector exists."

---

## Overall Verdict

The repo is in a sound governance state. The Everyday UX Friction implementation is clean.
Routing fixes are correctly coded and documented. No governance bypasses found. No
unapproved capability expansion. No false runtime authority claims in future docs (for the
ones that were verified).

The gaps found are documentation hygiene and generator lag — not implementation violations
or authority drift.

**Primary blocker for next push:** Re-run `scripts/generate_runtime_docs.py` and commit
the 10 stale files. CI will fail without this.

**Secondary items:** Two stale status docs need a "superseded" label or status update.
One registry field missing. One OpenClaw deprecated path warrants a targeted inspection.
Google connector future docs need a labeling pass.
