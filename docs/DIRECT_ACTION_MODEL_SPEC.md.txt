Excellent prompt. You’re asking for a **merge + arbitration**, not a rewrite.
Here is the **clean, authoritative merge** of your **Direct-Action Model** with the **Immediate Recommendations**, with **phase corrections applied** and **unsafe items deferred**.

I’ll be explicit about **what is approved now**, **what is documentation-only**, and **what must wait**.

---

# 🔒 Nova Direct-Action Model — **FINAL MERGED MAP**

**(Authoritative, Phase-Correct, Lock-Ready)**

This document replaces all previous partial notes.
Treat this as **canonical truth** going forward.

---

## 1️⃣ CORE MODEL — LOCKED (Design Truth)

### **Principle**

> **A user command may count as permission only when the action is safe, deterministic, and unambiguous.**

If any condition fails → **clarify or confirm**.
Never guess. Never infer. Never chain.

---

## 2️⃣ ACTION CLASSES — LOCKED (Future Behavior Model)

### 🅰️ Class A — Informational (No Permission Ever)

**Phase:** Already live
**Examples:** `time`, `date`, `weather`, `news`
**Rule:** Immediate response, no confirmation

---

### 🅱️ Class B — Direct Safe Actions (Intent = Permission)

**Phase:** **Future (Phase 3+) ONLY**

**Examples:**

* `open chrome`
* `open documents folder`

**Rules:**

* Explicit allow-list
* Exact match only
* One action
* No confirmation
* Immediate execution
* End interaction

🚫 **Not allowed in Phase 2 / 2.5**

---

### 🅲 Class C — Ambiguous Actions (Clarification Required)

**Examples:**

* `open docs`
* `open report`

**Rule:**
Ask one clarification. No execution.

---

### 🅳 Class D — Sensitive / Irreversible (Always Confirm)

**Examples:**

* delete
* move
* overwrite
* system changes

**Rule:**
Confirmation is **never removed**, even in future phases.

---

## 3️⃣ PHASE REALITY WALL — LOCKED

### **Phase 2 / 2.5 (Current Reality)**

* Intent ≠ Permission
* All actions (except Class A) require confirmation
* Exact matching only
* No inference
* No shortcuts

### **Phase 3+ (Future Capability)**

* Introduce Class B
* Still no guessing
* Still deterministic
* Still auditable

🚫 **No partial adoption allowed**

---

## 4️⃣ IMMEDIATE ACTIONS — REVIEWED & CORRECTED

This is where we merge your recommendations **with phase discipline enforced**.

---

### ✅ APPROVED NOW (SAFE, PHASE-2.5 COMPLIANT)

#### ✅ A. Create the Spec Document (DO THIS)

**Approved exactly as written**

```powershell
cd C:\Nova-Project\nova_backend
mkdir docs
```

Save as:

```
docs/DIRECT_ACTION_MODEL_SPEC.md
```

This is **documentation only**. No behavior change.

---

#### ✅ B. Keep Help + Cancel (Already Locked)

* `help` ✅
* `cancel / never mind` ✅

No changes needed.

---

### ❌ NOT APPROVED NOW (Must Be Deferred)

These were **good ideas**, but they **cross the phase boundary**.

---

#### ❌ 1. Friendlier Confirmation Wording — **DEFER**

> “I can open Chrome for you. Ready?”

**Why deferred:**

* Confirmation wording is part of the **authority UX contract**
* Changing it mid-Phase 2 risks:

  * breaking tests
  * misrepresenting permission semantics
* This belongs in **Phase 3 UX alignment**, not Phase 2.5

🔒 **Decision:** Defer

---

#### ❌ 2. Error Message Softening — **DEFER**

> “I didn’t catch that. Could you try again?”

**Why deferred:**

* Changes refusal semantics
* Introduces conversational tone drift
* Phase 2 requires **boring, explicit failures**

🔒 **Decision:** Defer

---

### ⚠️ CONDITIONALLY ALLOWED (DOCUMENTATION ONLY)

#### ⚠️ Action Classification Constants

Your idea is **excellent**, but must be **non-operative**.

✅ **Allowed if and only if**:

* Placed in `/docs` or comments
* NOT imported by runtime logic
* NOT referenced by routing

❌ **Not allowed** in `action_types.py` yet

**Approved alternative:**

```
docs/ACTION_CLASS_MAP.md
```

Or embedded inside:

```
docs/DIRECT_ACTION_MODEL_SPEC.md
```

---

## 5️⃣ SAFETY TEST — PARTIALLY APPROVED

Your instinct is right, but **code placement is premature**.

### ✅ Approved Concept

* Verifying that only time/date are read-only
* Ensuring everything else requires confirmation

### ❌ Not Approved Yet

* Adding new test files that imply behavior change

🔒 **Decision:**
Convert this into a **manual checklist** for now.
Automated test comes in Phase-3 refactor.

---

## 6️⃣ FINAL RECOMMENDED ORDER (CLEAN & SAFE)

### ✅ RIGHT NOW (10–15 min)

1. Create `docs/DIRECT_ACTION_MODEL_SPEC.md`
2. Paste the full model (Class A–D, rules, phases)
3. Save and lock

---

### 🔒 REMAINDER OF PHASE 2

4. Finish `OPEN_APP` (Chrome) **with confirmation**
5. Freeze Phase 2 execution
6. Snapshot

---

### ▶️ PHASE 3 ENTRY (Later)

7. Introduce Class B (intent = permission)
8. Update confirmation UX
9. Improve error phrasing
10. Add action classification constants
11. Add automated safety tests

---

## 7️⃣ FINAL VERDICT (Straight Talk)

You are **thinking exactly like a system architect**, but:

* Some of your recommendations are **Phase-3 ideas**, not Phase-2.5 actions
* The **design is correct**
* The **timing needed correction**
* Now it’s clean, safe, and aligned

---

## ✅ YOUR NEXT CONCRETE STEP (ONE LINE)

> **Create and lock `docs/DIRECT_ACTION_MODEL_SPEC.md`.**

When that’s done, tell me:

> “Spec document created.”

Then we return to **finishing OPEN_APP correctly** and close Phase 2 with confidence.
