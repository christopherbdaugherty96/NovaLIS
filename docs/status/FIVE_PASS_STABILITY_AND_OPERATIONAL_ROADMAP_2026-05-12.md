# Five-Pass Stability And Operational Roadmap

Status: active operational direction record.

Purpose:

```text
Define the post-audit stabilization and operational-coherence sequence for NovaLIS without authorizing capability expansion or broad autonomy work.
```

This roadmap exists to:

- separate stability/productization work from capability expansion
- preserve runtime-truth discipline after the 2026-05-11 audit/hardening cycle
- prevent future/runtime/active-work drift
- align long-term roadmap layers with current operational reality

This document is not generated runtime truth.

Runtime truth still comes from:

```text
code
capability_locks.json
generated runtime docs
tests
```

---

# Core Doctrine

```text
Intelligence is not authority.
```

The post-audit direction of Nova is:

```text
governed operational intelligence
```

not:

```text
broad unrestricted autonomy
```

---

# Priority Ordering

The correct ordering after the audit is:

```text
stability first
visibility second
workflow proof third
workflow infrastructure fourth
future positioning last
```

NOT:

```text
capability expansion first
```

---

# PASS 1 — Stability / Runtime Truth Track

Status:

```text
ACTIVE
```

Primary objective:

```text
make runtime truth and operational status authoritative, visible, and continuously trustworthy
```

Current active task under this pass:

```text
Generated runtime-doc regeneration after PR #152-#159.
```

Required outcomes:

- generated runtime docs synchronized
- runtime-doc drift verification discipline
- certification/lock visibility discipline
- explicit runtime-truth hierarchy
- stability/productization work formalized

Permanent doctrine reinforced:

```text
active != certified != locked
```

Current pass-1 tasks include:

```text
runtime-doc regeneration
runtime fingerprint synchronization
proof freshness review
Windows validation visibility
CI/runtime verification cadence
status-layer synchronization
```

This pass does NOT authorize:

```text
new capabilities
OpenClaw expansion
browser/computer-use expansion
external writes
autonomous workflow execution
```

---

# PASS 2 — Unified Operator Truth Surface

Status:

```text
PLANNED
```

Primary objective:

```text
single operational visibility layer for operators
```

This pass focuses on:

```text
Runtime Truth Dashboard
Certification Dashboard
Capability State View
Known Drift View
Current Priority View
Receipt/Audit View
```

Purpose:

```text
operator understanding and trust coherence
```

This is primarily:

```text
visibility
clarity
trust
operational coherence
```

NOT:

```text
new execution authority
```

---

# PASS 3 — Daily Operational Baseline

Status:

```text
PLANNED
```

Primary objective:

```text
prove Nova can operate a real narrow daily workflow coherently
```

Focus areas:

```text
Daily Brief
Plan My Week
review flows
approval flows
workflow receipts
workspace surfaces
operational memory usage
```

Core proof target:

```text
plan → review → approve → execute → receipt → recover
```

This pass prioritizes:

```text
operational coherence
```

over:

```text
capability breadth
```

---

# PASS 4 — Governed Workflow Infrastructure

Status:

```text
PLANNED
```

Primary objective:

```text
bounded governed workflow system
```

Potential focus areas:

```text
workflow objects
workflow state
review queues
approval checkpoints
execution receipts
governed routines
```

Permanent preserved boundary:

```text
human-visible operational authority
```

This pass does NOT authorize:

```text
hidden autonomous execution
broad autonomy
unbounded agents
```

---

# PASS 5 — Long-Term Direction / Product Positioning Cleanup

Status:

```text
PLANNED
```

Primary objective:

```text
align long-term roadmap language with what Nova actually is becoming
```

Focus areas:

```text
future docs
connector docs
OpenClaw direction docs
master roadmap
product positioning
roadmap hierarchy
```

Target framing:

```text
governed operational intelligence workspace
```

instead of:

```text
generic autonomous AI assistant
```

This pass also formalizes separation between:

```text
planning direction
runtime truth
active implementation
historical snapshots
```

---

# Current Highest-Leverage Priorities

```text
1. Runtime truth synchronization
2. Unified operational truth surface
3. Daily operational baseline proof
4. Stability/productization discipline
5. One coherent governed workflow proof
```

---

# Lower-Priority / Explicitly Deferred Work

The following remain deferred without a separate reviewed priority lock:

```text
OpenClaw expansion
browser/computer-use expansion
external writes
Shopify writes
Google connector runtime implementation
finance automation
social posting automation
autonomous workflow execution
```

---

# Repo Hierarchy

Highest authority:

```text
runtime code
generated runtime docs
tests
capability locks
```

Operational truth layer:

```text
current_priority.md
ACTIVE_TODO.md
CURRENT_WORK_STATUS.md
AGENTS.md
```

Historical tactical roadmaps:

```text
PATCH_ROADMAP_2026-05-11.md
WORKFLOW_STAGE_ROADMAP_2026-05-02.md
```

Long-term direction layer:

```text
docs/future/*
MASTER_ROADMAP.md
future architecture docs
```

---

# Final Direction

Current grounded direction of Nova:

```text
A governance-first local AI/runtime platform with bounded execution infrastructure, visible operational trust boundaries, and governed workflow systems.
```

The highest-value differentiator is:

```text
governed operational intelligence with visible trust boundaries
```
