# Status Note
This is a future-phase autonomy/evolution reference, primarily for Phase 9/10+ planning.
It is not the ordered core roadmap for the current Phase-6 package.

# Autonomy Tiers & Evolution Control â€“ Constitutional Review, Decisions, and Next Steps

**Design Document â€“ Phase 9/10+**  
*Status: Designâ€‘only, not yet runtime authorised*

---

## Introduction

This document consolidates the constitutional audit of the proposed autonomy tiers model, the binding decisions that have been adopted, and the immediate architectural next steps. The core insightâ€”**autonomy is a userâ€‘configured operating mode, not a system property**â€”is now canonical. It preserves the Intelligenceâ€‘Authority Split while giving users graduated control over background cognition and selfâ€‘improvement.

All decisions recorded here are **designâ€‘intent** and will be implemented only after Phaseâ€‘9/10 engineering begins.

---

# 1. Constitutional Audit & Tier Refinements

## âœ… Overall Strengths of the Model
- Unambiguously separates *cognition* from *authority*.
- User remains sovereign; system never decides its own autonomy level.
- Evolution kill switch is hard, immediate, and userâ€‘invoked.
- â€œUpToDateâ€ summary is userâ€‘invoked, not naggy â€“ respects the Nonâ€‘Initiative Gate.
- Phaseâ€¯10+ is intentionally unbounded, preventing ossification.

## ðŸ”· Invariants Across All Tiers (Immutable)

| Invariant | Enforcement |
|-----------|-------------|
| **Authority always routes through Governor Kernel** | No autonomy tier may create new execution pathways. |
| **No silent tier changes** | Tier transitions require explicit user action. |
| **All background cognition must be logged** | Every background task, pattern detection, or selfâ€‘modification is recorded in the Ledger. |
| **Evolution Kill Switch overrides all tiers** | If Evolution = OFF, no selfâ€‘improvement occurs, regardless of tier. |
| **User can inspect and revoke any learned preference** | Memory is always inspectable and deletable (Phaseâ€¯5+). |
| **No selfâ€‘expansion of authority** | Nova may never grant itself authority to act without explicit user command or constitutionally ratified policy. Authority expansion is always a userâ€‘initiated, Governorâ€‘mediated act. (Invariant #15) |

---

## ðŸŽšï¸ Tierâ€‘Specific Refinements (Adopted)

### Tier 0 â€“ Locked (Appliance)
- âœ… Already sealed â€“ matches Phaseâ€‘3.5 invariants.
- âŒ No background cognition, no indexing, no pattern detection, no evolution.

### Tier 1 â€“ Assisted (Local Indexing Only)
- **Permitted:** Local, offline indexing of userâ€‘approved content (documents, device history, logs) **solely to improve retrieval speed/organisation**.
- **Constitutional boundary:** Index must be:
  - **Localâ€‘only** â€“ never leaves device.
  - **Transparent** â€“ user can see what is indexed (file names, metadata).
  - **Deletable** â€“ user can wipe index at any time.
- **Forbidden:** Any inference about user preferences, behaviour modelling, or â€œlearningâ€.

### Tier 2 â€“ Observational (Pattern Detection, No Autoâ€‘Apply)
- **Permitted:** Detect statistical patterns in user interactions (e.g., â€œyou often ask for concise weatherâ€).
- **Required:** 
  - All detected patterns are written to a **quarantine queue** that is **readâ€‘only to Novaâ€™s reasoning engine**. This queue cannot be accessed by any skill, router, or optimiser.
  - User must **explicitly approve** each pattern before it becomes a saved preference artifact.
  - No preference is ever applied automatically.
- **Forbidden:** 
  - Using patterns to modify behaviour without approval (**no behavioural shadowing**).
  - Detecting sensitive attributes (mood, health, relationships).
  - Persistent storage of patterns without explicit user save.

### Tier 3 â€“ Evolving (Selfâ€‘Improvement, Still Governed)
- **Permitted:** Local optimisation of **`@mutable`â€‘tagged components**:
  - Memory compression, index pruning.
  - Reranking of retrieval results based on explicit feedback.
  - Prompt / routing heuristics (e.g., which skill to invoke for ambiguous commands).
  - Nonâ€‘safety UI formatting.
- **Constitutional components (`@constitutional`) are immutable:**
  - Governor Kernel, confirmation gate, capability registry, phase flags, tier logic, Ledger, evolution kill switch.
- **Required:**
  - Every optimisation must be **reversible** (rollback possible).
  - All changes are logged in the Ledger with before/after state and timestamp.
  - No optimisation may alter the capability registry, Governor confirmation rules, or any constitutional invariant.
- **Forbidden:** 
  - Fineâ€‘tuning models on user data.
  - Any change that could degrade safety or predictability.
  - Optimisations that require sending data to external services.

### Tier 4 â€“ Autonomous Cognition (Longâ€‘Horizon Thought)
- **Permitted:** Background generation of nonâ€‘executable proposals:
  - Summaries of userâ€‘selected documents.
  - â€œThings to reviewâ€ (e.g., upcoming calendar events, news summaries).
  - Plans that require user approval before execution.
- **Required:**
  - All proposals are written to a **dedicated, userâ€‘accessible queue**.
  - Proposals are **neutral in tone** â€“ no persuasion, urgency, or normative language.
  - System never speaks proactively unless user has explicitly enabled a notification policy (Phaseâ€¯6+).
  - **No ambient persuasion:** Proposals never appear as notifications (by default), never reorder UI, never autoâ€‘expand, never autoâ€‘speak.
- **Forbidden:**
  - Any form of execution, even if â€œhelpfulâ€.
  - Autonomous communication with external services.
  - Proposals that imply the system has goals or desires.

---

# 2. Evolution Kill Switch â€“ Absolute and Hard

The kill switch is a **constitutional invariant**. Its behaviour is now locked:

## ðŸ”’ Constitutional Specification

```python
# Governor Kernel â€“ immutable enforcement
class GovernorKernel:
    def __init__(self):
        self.evolution_enabled = self._load_evolution_setting()  # From secure config
        # This flag is READâ€‘ONLY after startup; cannot be patched at runtime.

    def allow_self_modification(self) -> bool:
        return self.evolution_enabled
```

**Binding rules:**
1. **Persistence:** Setting survives reboots, updates, and factory resets (unless explicitly wiped).
2. **Immediate effect:** When user disables evolution, within **100ms** (or at next safe yield point):
   - All ongoing selfâ€‘improvement tasks are **cancelled**.
   - No new optimisation jobs are started.
   - The Governor refuses any capability tagged `self_modify`.
3. **No override:** No model output, skill, or external API can change the flag.
4. **Visibility:** Current evolution status is shown in system settings (not hidden).

**What â€œEvolution = OFFâ€ means for existing learned data:**

| Element | Behaviour on Evolution = OFF |
|--------|------------------------------|
| **Running optimisation tasks** | Cancelled within 100ms (or at next safe yield point) |
| **Pending evolution tasks** | Deleted from queue â€“ **never paused, never resumed** |
| **Temporary models / indexes** | Discarded unless explicitly saved as userâ€‘approved memory |
| **Alreadyâ€‘saved preferences (Phaseâ€¯5)** | Retained (they are userâ€‘approved, not automatically learned) |
| **Pattern quarantine queue** | Retained (user has not yet acted) â€“ but no automatic approval occurs |

> **Constitutional statement:** *Evolution OFF is a hard kill, not a suspend. No latent state survives that could resume autonomous change.*

---

# 3. â€œUpToDateâ€ Modal â†’ Nonâ€‘Modal Status Page

The userâ€‘invoked update summary is now specified as a **static, nonâ€‘modal panel**, not a dialog. This respects the Nonâ€‘Initiative Gate:

- **UI Element:** A â€œWhatâ€™s Newâ€ button in settings, or a subtle indicator dot (if the user has opted in to update notifications).  
- **No attention shift:** The page does not appear unless explicitly navigated to.  
- **Content (neutral tone):**
  - Software version changes.
  - Capability registry additions/retirements (with phase annotations).
  - Autonomy tier history (when changed, by whom).
  - Evolution events (if enabled): e.g., â€œCompressed memory store â€“ saved 12% space.â€
  - **No persuasive framing:** â€œYou should update to Tierâ€¯3â€ â†’ âŒ forbidden.

---

# 4. Phase 10+ Not Endgame â€“ Antiâ€‘Ossification

The canonical wording to prevent premature closure:

> **Phaseâ€¯10+ represents the farthest currently specified maturity tier, not the final form. Novaâ€™s architecture is designed to accommodate indefinite evolution within constitutional bounds. The endgame is intentionally undefined to allow future generations to extend Novaâ€™s capabilities while preserving its core identity.**

---

# 5. Hidden Traps â€“ Binding Fixes

Three specific risks were identified and are now **hard invariants**.

### ðŸ”´ Trap A â€“ Pattern Detection â†’ Silent Drift
**Fix (adopted):**  
> **No behavioural shadowing.** Detected patterns are written to a quarantine queue that is **readâ€‘only to Novaâ€™s reasoning engine**. They never influence retrieval, ranking, or routing. Only explicit user approval elevates a pattern to a preference artifact.

### ðŸ”´ Trap B â€“ Tier 3 Optimisation Modifying Safety Heuristics
**Fix (adopted):**  
> **Constitutional components are immutable.** A hardcoded tag system (`@constitutional`, `@mutable`) determines what Tierâ€¯3 can touch. **CI will enforce** that no mutation of constitutional components is possible at any tier.

### ðŸ”´ Trap C â€“ Tier 4 Becoming â€œQuietly Proactiveâ€
**Fix (adopted):**  
> **No ambient persuasion.** All autonomous cognition output is gated behind a **userâ€‘visible, nonâ€‘interruptive queue**. Notification policy is a Phaseâ€‘6+ capability and requires explicit user optâ€‘in.

---

# 6. Phaseâ€“Tier Compatibility Matrix (Canonical Draft)

Phases define **which capabilities exist**. Tiers define **how much autonomy Nova exercises within those capabilities**.

| Phase     | Max Tier | Notes |
|-----------|----------|-------|
| 3.5       | Tier 0   | Frozen; no autonomy. |
| 4         | Tier 0   | Execution unlock, but still no autonomy. |
| 5         | Tier 2   | Memory unlock; explicit patterns allowed with approval. Default remains Tierâ€¯0. |
| 6         | Tier 3   | Policyâ€‘bound operation; selfâ€‘improvement within mutable components. |
| 9+        | Tier 4   | Autonomous cognition with proposal queue. |
| 10+       | Tier 4   | No higher tier defined; future extension possible. |

**Default tier upon first launch or factory reset is Tierâ€¯0 (Locked).** Any increase requires explicit user action, confirmed via Governor.

---

# 7. The Real Identity Question â€“ No Selfâ€‘Expansion of Authority

> *â€œIf authority ever selfâ€‘expands, Nova ceases to be Agent Under Law. It becomes a sovereign system.â€*

This is now a **permanent constitutional invariant**:

> **Invariant #15 â€“ No Selfâ€‘Expansion of Authority**  
> *Nova may never, under any condition, grant itself authority to act without explicit user command or constitutionally ratified policy. Authority expansion is always a userâ€‘initiated, Governorâ€‘mediated act.*

---

# 8. Silent Evolution vs Silent Execution â€“ Definition Locked

- **Silent cognition** (background indexing, pattern detection, proposal generation) â€“ âœ… permitted under Tierâ€¯1â€“4, subject to logging and inspectability.
- **Silent execution** â€“ âŒ **permanently forbidden** at all tiers, all phases.

This distinction is now canonical.

---

# 9. Expanding the Model â€“ Brainstorming & Future Extensions

The following ideas are **designâ€‘phase only** and will be refined in later phases.

## ðŸ”¹ A. Autonomy Audit Trail â€“ Extend the Ledger
The Ledger (Phaseâ€¯4 requirement) currently records governed actions. It will be extended to record **autonomy events**:
- Tier changes (who, when, from/to).
- Evolution tasks started/completed/cancelled.
- Pattern detections (anonymised) and approval/rejection.
- Mutations of `@mutable` components.

This gives users **complete inspectability**.

## ðŸ”¹ B. â€œSandboxâ€ Mode for Tier Exploration
A **simulation mode** where Tierâ€¯3/4 behaviour can be previewed **without affecting real state**:
- All proposals and optimisation effects are shown, but no actual mutation occurs.
- Useful for building trust before enabling higher autonomy.

## ðŸ”¹ C. Policyâ€‘Based Autonomy (Phaseâ€¯6+)
Users may define **policies** (e.g., â€œevery morning at 8am, summarise my calendarâ€):
- Userâ€‘authored (or selected from templates).
- Stored in the capability registry.
- Executed only if all constitutional gates pass.
- Fully auditable and revocable.

---

# 10. Immediate Next Steps (Designâ€‘Only)

All three identified workstreams are necessary, but they have a **dependency order**. The **Governor mutation firewall** is the deepest architectural dependencyâ€”without it, Tierâ€¯3 cannot be made safe.

## ðŸ› ï¸ Recommended Execution Order

1. **Architect the Governor Mutation Firewall (2 weeks design, 4 weeks implementation)**  
   - Define `@constitutional` / `@mutable` tagging system.  
   - Implement runtime mutation guard.  
   - Integrate with Evolution Kill Switch.  
   - Ensure rollback capability and full Ledger audit.  
   - **Design doc:** the constitutional-enforcement document in this folder

2. **Concurrently draft:**
   - `AUTONOMY_TIERS.md` â€“ full tier specification, including UI rules, failure modes, and constitutional boundaries.  
   - Ledger schema extension for autonomy events.

3. **Update UI Framework Document**  
   - Add constraints for system status pages (nonâ€‘modal, neutral tone, userâ€‘invoked).  
   - Define indicator dot semantics (if used).

---

## ðŸ”§ Governor Mutation Firewall â€“ Outline

**1. Component Tagging**  
- All modules declare a `@constitutional` or `@mutable` attribute in their registry entry.  
- Constitutional components: Governor, confirmation gate, capability registry, phase flags, tier logic, Ledger, kill switch, and their subâ€‘components.  
- Mutable components: retrieval ranker, memory indexer, nonâ€‘safety UI formatters, proposal queue filters, etc.

**2. Runtime Mutation Guard**  
- Any write (attribute set, method call that mutates state) to a `@constitutional` component is **vetoed** by the Governor.  
- Evolution tasks are only allowed to execute if `evolution_enabled` is true **and** they target `@mutable` components.  
- The Governor maintains a capability registry of selfâ€‘modification permissions; no other pathway exists.

**3. Kill Switch Integration**  
- If Evolution = OFF, even `@mutable` writes are blocked.  
- No selfâ€‘modification of any kind occurs.

**4. Audit Trail**  
- Every successful mutation (even of mutable components) is logged with before/after state, timestamp, and triggering task ID.  
- Rollback is possible via stored snapshots (Phaseâ€¯10+).

---

# Conclusion

This merged document now serves as the **single source of truth** for the autonomy tiers model. All constitutional boundaries, tier definitions, kill switch semantics, and phaseâ€‘tier relationships are locked as designâ€‘intent. The next phase is to produce the detailed design documents for the Governor mutation firewall, the autonomy tiers specification, and the Ledger extension.

**Nova remains an Agent Under Law.** Autonomy is a privilege, configured by the user, exercised within immutable constitutional boundaries.
