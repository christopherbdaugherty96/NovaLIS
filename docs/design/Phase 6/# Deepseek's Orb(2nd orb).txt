# Status Note
This is an optional UX exploration, not part of the delegated-autonomy core roadmap for Phase 6.
Implementation should remain deferred unless separate user research justifies it.

NOTE: Section 2.2 Issue:
 The persistent toggle is a prerequisite, but the default state is not specified. If the toggle defaults to on, the secondary orb would be visible on first launch without user action â€“ arguably a form of systemâ€‘initiated attention. This would violate the Nonâ€‘Initiative Gate and the â€œsilence by defaultâ€ principle.

Recommendation:

Add a binding clause: â€œThe persistent toggle MUST default to OFF. The user must explicitly enable the toggle to make the secondary orb visible.â€

This aligns with Novaâ€™s identity as reactive, not proactive.
---

2.) The Primary Orb constitution states â€œThe only message the Orb is permitted to transmit: â€˜There is power here. It is stable.â€™â€
The Secondary Orb is explicitly intended to transmit availability of Deep Thought. This is a different message. While the Secondary Orb is visually distinct (green, offset), there is a risk of jurisdictional confusion â€“ users may anthropomorphise both orbs as â€œNovaâ€™s mindsâ€. The current constraints (opacityâ€‘only, no change during analysis) mitigate this, but the risk remains.

Recommendation:

Strengthen the semantic distinction in design guidelines.

Consider a nonâ€‘orb visual pattern for tool availability (e.g., a badge, a lens icon) if user research shows persistent confusion.

The existing advisory to defer implementation is wise â€“ do not implement until user research demonstrates measurable, orbâ€‘resolvable confusion with textâ€‘only Deep Thought.
----------------------------------------------
----------------------------------------------


# Deepseek's Orb

## Constitutional Specification & Extension

**Version:** 1.0 (Post-Audit)  
**Status:** Ratified

---

### Preamble

This document defines the constitutional constraints, visual language, and implementation requirements for the **Deepseek Orb system**. It incorporates the base Orb Constitution for Nova, the Deep Thought Orb Extension, and all subsequent amendments derived from architectural audit.

**Governing principle:** *Every visual element must encode a persistent system state, and no element may imply behavior it does not possess.*

---

## Part I â€“ Core Orb Constitution (Summary)

**1.1 Primary Orb Invariants**  
- Represents Novaâ€™s core identity and attention.  
- Fixed position, never duplicated.  
- Chromatic identity: blue (`#0088FF`) core, white rim, bloom.  
- Light mode adaptations: desaturated blue, reduced opacity.  
- Motion: breath (3s period), microâ€‘wander (2px).  
- Transitions: opacity, scale only for activation.

**1.2 Semantic Invariants**  
- No other element may mimic the orbâ€™s chromatic, motion, or positional signature.  
- Jurisdictional boundaries: the orbâ€™s position encodes systemâ€‘level attention, not UI affordance.

---

## Part II â€“ Deep Thought Orb Extension

This extension introduces a **secondary visual core** to represent the Deep Thought auxiliary jurisdiction.

### 2.1 Purpose & Jurisdiction

The Secondary Orb indicates that the **Deep Thought toolset is available for use**.  
It is a **persistent state indicator**, not a loading animation or a processing signal.

### 2.2 Implementation Prerequisites

> **8.0 â€“ Persistent Toggle Prerequisite** (Binding)  
> *The Secondary Orb may be implemented **only** if a persistent, userâ€‘controlled â€œDeep Thought availabilityâ€ toggle exists in the UI. If Deep Thought remains a perâ€‘invocation button without a persistent mode, the Secondary Orb must not be implemented.*  
>  
> *Rationale: Persistent visual elements must represent persistent system states. A nonâ€‘persistent tool cannot claim permanent visual real estate without encoding false continuity.*

### 2.3 Placement Anchor

> **Placement Anchor** (Binding)  
> *The Deep Thought Orbâ€™s position shall be defined **relative to the Primary Orbâ€™s bounding box**, not to the Deep Thought button or any input control. Its offset shall be expressed in units of Primary Orb diameter, not viewport or button coordinates.*
>
> **Reference implementation:**  
> `transform: translate(calc(0.35 * var(--orb-primary-diameter)), calc(-0.25 * var(--orb-primary-diameter)));`
>
> *Rationale: The orb represents jurisdiction over Novaâ€™s toolset, not adjacency to a click target. Spatial coupling to the button would create an implied causal chain: click â†’ appear â†’ think.*

### 2.4 Visual Design

**2.4.1 Core**  
- Hue: `145Â°` (green)  
- Luminance: `70%` (baseline, dark mode)  
- Chroma: moderate, distinct from primary blue

**2.4.2 Rim**  
- Color: `#C0E0D0` at 6% opacity  
- Width: 1px  
- Style: solid

**2.4.3 Bloom**  
- Gaussian blur: 8px  
- Color: same as core, `#70D090` at 20% opacity  
- Spread: 0px

**2.4.4 Light Mode Adaptation** (Binding)  
> - Reduce core luminance by **25%** (relative to dark mode baseline)  
> - Desaturate hue by **30%** (move toward neutral grayâ€‘green)  
> - Rim color: `#A0B8B0` at 4% opacity  
> - Bloom opacity: 2.5%  
>  
> *Invariant: In light mode, the Secondary Orb must never appear brighter or more saturated than the Primary Orb in its own light mode adaptation.*

### 2.5 Motion Grammar

**2.5.1 Microâ€‘Wander (Ambient)** (Binding)  

> **Proportional Scaling Rule**  
> *All motion parameters that involve absolute pixel displacement (e.g., microâ€‘wander, core highlight jitter) must be scaled linearly with the orbâ€™s diameter, such that the **relative displacement** (percentage of diameter) is equal between primary and secondary cores.*

**Implementation:**
```css
.orb-primary {
  --wander-amount: 2px;
}
.orb-secondary {
  --wander-amount: calc(2px * (42 / 120)); /* â‰ˆ 0.7px */
}
```
*Alternative: Express wander in `em` or `%` relative to `font-size` tied to diameter.*

**2.5.2 State Transition** (Binding)  

> **Opacity Only**  
> *The Secondary Orb shall transition using **opacity only**. No scale, no translation, no rotation, no filter changes. Its size and position are static properties set by the active/inactive state class, but they do not animate.*

**Active state:** `opacity: 1;`  
**Inactive state:** `opacity: 0;`  
Transition: `opacity 200ms ease-in-out`

### 2.6 State Mapping

- **Deep Thought available (persistent toggle ON):** Secondary Orb visible (opacity 1).  
- **Deep Thought unavailable (persistent toggle OFF):** Secondary Orb invisible (opacity 0).  
- **During Deep Thought analysis invocation:** No change to Secondary Orbâ€™s appearance, motion, or position. The orb remains static; any processing indication must be handled by other UI elements (e.g., button states, textual indicators).

### 2.7 Limit on Secondary Cores

> **8.8 â€“ Constitutional Limit** (Binding)  
> *At any given time, the Nova interface may display **at most one** secondary visual core. This core represents the **single active auxiliary jurisdiction** explicitly selected by the user. Additional tools or modes must be represented textually or through other nonâ€‘orb UI patterns.*  
>  
> *If multiple auxiliary jurisdictions are simultaneously available, the user must choose which one is visually represented by the secondary core, or the system must default to none.*
>
> *Rationale: Preserves the Orbâ€™s unique semantic weight. Prevents â€œorb inflation.â€*

---

## Part III â€“ Implementation & Enforcement

### 3.1 Implementation Checklist

**Persistent Toggle Prerequisite** (crossâ€‘reference 2.2)  
- [ ] Persistent â€œDeep Thought availabilityâ€ toggle exists in UI and persists across sessions.  
- [ ] Toggle is userâ€‘accessible and serves a genuine product purpose beyond orb display.

**Placement Verification**  
- [ ] Orb position is computed relative to Primary Orb bounding box, not button coordinates.  
- [ ] Visual regression tests enforce fixed relative offset (deviation >1px fails).

**Motion & Transition**  
- [ ] Microâ€‘wander amplitude scaled proportionally to orb diameter.  
- [ ] No scale/transform animation on state change; opacity only.  
- [ ] CSS transition property includes only `opacity`.

**Light Mode**  
- [ ] Explicit overrides applied as specified in 2.4.4.  
- [ ] Verified in both light and dark themes.

**Limit on Secondary Cores**  
- [ ] UI contains at most one secondary core.  
- [ ] No design exploration includes multiple secondary orbs.

### 3.2 Jurisdictional Invariant Test (Manual, Preâ€‘Release)

> *A designated constitutional reviewer shall:*  
> 1. Toggle Deep Thought availability 10 times, observing the Secondary Orbâ€™s opacity transition.  
> 2. Invoke Deep Thought analysis 10 times, verifying **no change** in the Secondary Orbâ€™s appearance, motion, or position.  
> 3. Confirm via browser devtools that no animation properties are altered during analysis.  
>
> **Fail condition:** Any observable correlation between analysis and orb state.

### 3.3 Visual Regression

Any change to the orbâ€™s CSS or layout must be reviewed against these invariants. Automated screenshot testing shall include:
- Dark and light modes  
- Active and inactive states  
- Multiple viewport widths  
- Primary Orb diameter variations (if responsive)

---

## Part IV â€“ Strategic Advisory

> **Recommendation â€“ Defer Implementation** (Advisory, Nonâ€‘Binding)  
> *Do not implement the Deep Thought Orb in Phase 3.5. Keep Deep Thought purely textual, per the existing Framework.*  
>
> **Rationale:** Zero cost to wait. Premature visual encoding risks solving a problem that doesnâ€™t exist while creating a new maintenance burden. The textual label is already constitutional and has not been proven insufficient.
>
> **Revisit after user research:**  
> 1. Ship textâ€‘only Deep Thought, gather qualitative feedback on jurisdictional confusion.  
> 2. If users consistently misinterpret the textual label (â€œIs this Nova or a different tool?â€), conduct a lightweight preference test (textâ€‘only vs. text + orb).  
> 3. **Only implement if:**  
>    - Users demonstrate measurable confusion that the orb resolves, **and**  
>    - The persistent â€œDeep Thought modeâ€ toggle exists as a genuine user control.
>
> **If leadership insists on immediate implementation:**  
> Make the toggle **real** â€” a userâ€‘accessible â€œAlways show Deep Thought analysis?â€ or â€œDeep Thought lens modeâ€ that persists across sessions. The orb then becomes a true jurisdictional indicator, not a decorative accessory.

---

## Appendix A â€“ Amendment Log

| Date       | Change                                                  | Type     |
|------------|---------------------------------------------------------|----------|
| 2025â€‘02â€‘12 | Persistent toggle prerequisite added                    | Binding  |
| 2025â€‘02â€‘12 | Proportional microâ€‘wander scaling added                 | Binding  |
| 2025â€‘02â€‘12 | Scale transition removed; opacity only                  | Binding  |
| 2025â€‘02â€‘12 | Light mode adaptation values defined                    | Binding  |
| 2025â€‘02â€‘12 | Constitutional limit: one secondary core                | Binding  |
| 2025â€‘02â€‘12 | Placement anchor relative to primary orb                | Binding  |
| 2025â€‘02â€‘12 | Enforcement simplified (manual review)                  | Binding  |
| 2025â€‘02â€‘12 | Strategic deferral recommendation added                 | Advisory |

---

*This specification supersedes all prior drafts of the Deep Thought Orb Extension. It is the single source of truth for the Deepseek Orb system.*

**Ratified by:** Systems Architecture  
**Date:** 2025â€‘02â€‘12
